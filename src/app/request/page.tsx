"use client";

import RequestPaymentWeb from "~/components/web/RequestPaymentWeb";

// Page for users to request payment via the web app (not Frame)
export default function RequestPage() {
  return <RequestPaymentWeb />;
}
